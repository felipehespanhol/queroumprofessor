:css
  .footer { position: fixed; bottom: 0; }

.home-header
  - if session[:teacher]
    = link_to "logout", "/logout"
    %br
    = "Olá #{session[:teacher]}"

.home-search-box
  #messages
    -if flash[:notice]
      .alert.alert-success
        = flash[:notice]

  = render :partial => 'search_fields'


