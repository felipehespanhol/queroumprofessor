.min_search_bar
  = image_tag "queroumprofessor_branco.png"
  = render :partial => "search_fields"

%p

- if @teachers.nil? || @teachers.empty?
  .center
    %br
    .alert.alert-box
      Infelizmente nenhum professor foi encontrado em sua cidade... 

- else
  %h3 Encontre seu professor:

  /
    %hr
  - @teachers.each do |teacher|
    
    .span2
      =render :partial => "teacher_info", :locals => {:teacher => teacher}

